# Railway / Node production database. Railway injects this automatically when
# a PostgreSQL service is connected to the application service.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Optional. If omitted, GrowthOS opens the first active organization.
APP_ORGANIZATION_ID=
